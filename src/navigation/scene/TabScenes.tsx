import React, { FunctionComponent } from 'react';
import { View, Text } from 'react-native';

const MainTabContainer: FunctionComponent = () => {
    return (
        <View>
            <Text>RegisterScreen</Text>
        </View>
    );
};

export default MainTabContainer;
